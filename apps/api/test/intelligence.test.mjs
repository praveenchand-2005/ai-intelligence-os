import test from 'node:test';import assert from 'node:assert/strict';import { resolveEntity } from '../src/entity-resolution.mjs';import { buildEntityGraph } from '../src/entity-graph.mjs';import { analyzeGraph } from '../src/ai-analyst-v2.mjs';
const evidence=[{id:'e1',provider:'rdap',title:'Example domain',summary:'Example',url:'https://example.com',domain:'example.com'},{id:'e2',provider:'dns',title:'Example DNS',summary:'Example',url:'https://example.com/dns',domain:'example.com'}];
test('entity resolution detects cross-provider corroboration',()=>{const r=resolveEntity('example.com',evidence);assert.equal(r.corroborated,1);assert.equal(r.confidence,'high');assert.equal(r.candidates[0].sources.length,2)});
test('graph contains corroboration edge',()=>{const g=buildEntityGraph('example.com',evidence);assert.ok(g.nodes.length>=3);assert.ok(g.edges.some(e=>e.type==='corroborates'));assert.equal(g.stats.corroboratedCandidates,1)});
test('graph analyst produces evidence-linked finding',()=>{const g=buildEntityGraph('example.com',evidence);const a=analyzeGraph('example.com',g);assert.equal(a.mode,'graph-grounded');assert.ok(a.findings.length>0);assert.ok(a.findings[0].evidenceIds.length>=2)});
test('graph analyst stays low confidence without corroboration',()=>{const g=buildEntityGraph('other.com',[{id:'e1',provider:'rdap',title:'Other',domain:'other.com'}]);const a=analyzeGraph('other.com',g);assert.equal(a.confidence,'low');assert.ok(a.limitations.length>0)});
