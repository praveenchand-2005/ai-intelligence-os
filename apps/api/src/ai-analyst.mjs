function rankEvidence(evidence=[]){return [...evidence].sort((a,b)=>Date.parse(b.retrievedAt||0)-Date.parse(a.retrievedAt||0));}
export function analyzeEvidence(target,evidence=[]){const ranked=rankEvidence(evidence);const sources=[...new Set(ranked.map(e=>e.provider).filter(Boolean))];const high=ranked.filter(e=>e.summary&&e.url).length;const confidence=high>=3?'high':high>=1?'medium':'low';const findings=ranked.slice(0,10).map((e,i)=>({id:`finding:${i}`,statement:e.summary,provider:e.provider,source:e.url,retrievedAt:e.retrievedAt}));return {target,summary:findings.length?`Analysis grounded in ${findings.length} evidence item(s) from ${sources.length} source(s).`:'Insufficient current evidence for a grounded conclusion.',findings,sources,confidence,limitations:confidence==='low'?['Insufficient corroborating public evidence.','Do not treat absence of evidence as evidence of absence.']:[],generatedAt:new Date().toISOString()};}
