export function buildCaseReport(item,analysis){const findings=analysis?.findings||item.findings||[];return {id:`report:${item.id}`,caseId:item.id,title:`Intelligence Report — ${item.target}`,target:item.target,status:item.status,executiveSummary:analysis?.summary||'No analyst summary available.',confidence:analysis?.confidence||'low',findings,sourceCount:new Set((item.evidence||[]).map(e=>e.provider)).size,evidenceCount:(item.evidence||[]).length,limitations:analysis?.limitations||[],generatedAt:new Date().toISOString()};}
