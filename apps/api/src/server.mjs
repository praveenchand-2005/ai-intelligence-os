import http from 'node:http';import { readFile } from 'node:fs/promises';import { extname, join, normalize } from 'node:path';import { fileURLToPath } from 'node:url';import { createCase, updateCase, getCase, listCases } from './store.mjs';import { collect } from './investigation.mjs';import { buildEntityGraph } from './entity-graph.mjs';import { analyzeGraph } from './ai-analyst-v2.mjs';import { buildCaseReport } from './reporting.mjs';import { initDb, dbEnabled, saveGraph, getGraph, saveReport, getReport } from './db.mjs';import { register, login, authHeader } from './auth.mjs';import { providerHealth } from './provider-reliability.mjs';import { rateLimit, validateTarget, securityHeaders } from './security.mjs';import { createWatch, listWatches, pauseWatch, resumeWatch, watchdogStatus } from './watchdog.mjs';import { ensureWatchdogSchema, saveWatch } from './watchdog-store.mjs';import { listAlerts, markRead } from './alerts.mjs';import { runWatchWithChangeDetection } from './watchdog-engine.mjs';import { startWatchdogDaemon } from './watchdog-daemon.mjs';
const port=Number(process.env.PORT||8787);const webRoot=fileURLToPath(new URL('../../web/',import.meta.url));const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8'};const json=(res,s,b,extra={})=>{if(res.headersSent)return;res.writeHead(s,{'content-type':'application/json; charset=utf-8','cache-control':'no-store',...securityHeaders(),...extra});res.end(JSON.stringify(b))};const read=req=>new Promise((resolve,reject)=>{let b='';req.on('data',c=>{b+=c;if(b.length>1_000_000)req.destroy(Error('request too large'))});req.on('end',()=>{try{resolve(b?JSON.parse(b):{})}catch(e){reject(e)}});req.on('error',reject)});const staticFile=async(res,p)=>{try{const safe=normalize(p).replace(/^([.][.][/\\])+/, '');const f=join(webRoot,safe==='/'?'index.html':safe.replace(/^[/\\]/,''));const body=await readFile(f);res.writeHead(200,{...securityHeaders(),'content-type':types[extname(f)]||'application/octet-stream'});res.end(body);return true}catch{return false}};
const server=http.createServer(async(req,res)=>{try{const u=new URL(req.url||'/','http://localhost');const ip=req.headers['x-forwarded-for']?.split(',')[0]?.trim()||req.socket.remoteAddress||'unknown';const rl=rateLimit(`${ip}:${u.pathname}`,u.pathname.startsWith('/api/auth/')?20:60);if(!rl.ok)return json(res,429,{error:'rate limit exceeded',retryAfter:rl.retryAfter},{'retry-after':String(rl.retryAfter),'x-ratelimit-remaining':'0'});const auth=async()=>await authHeader(req);if(req.method==='GET'&&u.pathname==='/api/health')return json(res,200,{ok:true,service:'aio-api',mode:'live',persistence:dbEnabled()?'postgres':'memory',providers:providerHealth()});if(req.method==='GET'&&u.pathname==='/api/providers/health')return json(res,200,{items:providerHealth()});if(req.method==='POST'&&u.pathname==='/api/auth/register')try{const b=await read(req);return json(res,201,await register(String(b.email||''),String(b.password||'')))}catch(e){return json(res,400,{error:e.message})};if(req.method==='POST'&&u.pathname==='/api/auth/login')try{const b=await read(req);return json(res,200,await login(String(b.email||''),String(b.password||'')))}catch(e){return json(res,401,{error:e.message})};if(req.method==='GET'&&u.pathname==='/api/auth/me'){const s=await auth();return s?json(res,200,{authenticated:true,email:s.email}):json(res,401,{authenticated:false})};if(req.method==='GET'&&u.pathname==='/api/providers')return json(res,200,{items:[{id:'iana-rdap',name:'IANA RDAP',status:'operational'},{id:'cloudflare-doh',name:'Cloudflare DNS-over-HTTPS',status:'operational'},{id:'email-dns',name:'Email DNS resolver',status:'operational'}]});if(req.method==='GET'&&u.pathname==='/api/cases'){if(!await auth())return json(res,401,{error:'authentication required'});return json(res,200,{items:await listCases()})};if(u.pathname==='/api/watchdog'&&req.method==='GET'){if(!await auth())return json(res,401,{error:'authentication required'});return json(res,200,{items:listWatches(),status:watchdogStatus()})};if(u.pathname==='/api/watchdog'&&req.method==='POST'){if(!await auth())return json(res,401,{error:'authentication required'});try{const w=createWatch(await read(req));await saveWatch(w);return json(res,201,w)}catch(e){return json(res,400,{error:e.message})}};const wm=u.pathname.match(/^\/api\/watchdog\/([^/]+)\/(run|pause|resume)$/);if(wm){if(!await auth())return json(res,401,{error:'authentication required'});const id=wm[1],action=wm[2];if(action==='pause'||action==='resume'){const w=action==='pause'?pauseWatch(id):resumeWatch(id);if(w)await saveWatch(w);return w?json(res,200,w):json(res,404,{error:'watch not found'})}const out=await runWatchWithChangeDetection(id,async target=>{const r=await collect(target);return {kind:r.kind,evidence:r.evidence,result:r.result}});return out?json(res,200,out):json(res,404,{error:'watch not found or inactive'})};if(req.method==='GET'&&u.pathname==='/api/alerts'){if(!await auth())return json(res,401,{error:'authentication required'});return json(res,200,{items:listAlerts()})};const ar=u.pathname.match(/^\/api\/alerts\/([^/]+)\/read$/);if(req.method==='POST'&&ar){if(!await auth())return json(res,401,{error:'authentication required'});return json(res,200,markRead(ar[1]))};if(req.method==='POST'&&u.pathname==='/api/investigations'){if(!await auth())return json(res,401,{error:'authentication required'});try{const body=await read(req),target=validateTarget(body.target);const item=await createCase({target,status:'collecting',evidence:[],findings:[],sources:[]});try{const r=await collect(target);const saved=await updateCase(item.id,{status:r.error?'degraded':'completed',kind:r.kind,result:r.result,evidence:r.evidence,sources:[...new Set(r.evidence.map(e=>e.provider))],message:r.message,completedAt:new Date().toISOString(),error:r.error});if(dbEnabled())await saveGraph(item.id,buildEntityGraph(saved.target,saved.evidence||[]));return json(res,202,saved)}catch{return json(res,202,await updateCase(item.id,{status:'degraded',error:'Provider collection failed'}))}}catch(e){return json(res,400,{error:e.message||'invalid request'})}};const im=u.pathname.match(/^\/api\/investigations\/([^/]+)\/analysis$/);if(req.method==='GET'&&im){if(!await auth())return json(res,401,{error:'authentication required'});const item=await getCase(im[1]);if(!item)return json(res,404,{error:'not found'});const graph=(await getGraph(im[1]))||buildEntityGraph(item.target,item.evidence||[]);return json(res,200,analyzeGraph(item.target,graph))};const rr=u.pathname.match(/^\/api\/investigations\/([^/]+)\/report$/);if(rr&&req.method==='GET'){if(!await auth())return json(res,401,{error:'authentication required'});const id=rr[1],existing=await getReport(`report:${id}`);if(existing)return json(res,200,existing);const item=await getCase(id);if(!item)return json(res,404,{error:'not found'});const report=buildCaseReport(item,analyzeGraph(item.target,(await getGraph(id))||buildEntityGraph(item.target,item.evidence||[])));if(dbEnabled())await saveReport(report);return json(res,200,report)};if(req.method==='GET'&&u.pathname.startsWith('/api/investigations/')&&u.pathname.endsWith('/graph')){if(!await auth())return json(res,401,{error:'authentication required'});const id=u.pathname.split('/')[3];const persisted=await getGraph(id);if(persisted)return json(res,200,persisted);const item=await getCase(id);return item?json(res,200,buildEntityGraph(item.target,item.evidence||[])):json(res,404,{error:'not found'})};if(req.method==='GET'&&u.pathname.startsWith('/api/investigations/')){if(!await auth())return json(res,401,{error:'authentication required'});const item=await getCase(u.pathname.split('/').pop());return item?json(res,200,item):json(res,404,{error:'not found'})};if(req.method==='GET'&&await staticFile(res,u.pathname))return;return json(res,404,{error:'not found'})}catch(e){if(!res.headersSent)json(res,500,{error:'internal server error'});else req.destroy()}});initDb().then(async()=>{await ensureWatchdogSchema();server.listen(port,()=>{startWatchdogDaemon();console.log(`AIO listening on ${port}`)})}).catch(e=>{console.error('db init failed',e);server.listen(port,()=>{startWatchdogDaemon();console.log(`AIO listening on ${port} without database`)})});