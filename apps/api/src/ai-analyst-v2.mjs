export function analyzeGraph(target,graph){const nodes=graph?.nodes||[],edges=graph?.edges||[],evidence=nodes.filter(n=>n.type==='evidence');const corroborated=edges.filter(e=>e.type==='corroborates');const findings=[];for(const edge of corroborated.slice(0,10))findings.push({id:`graph-finding:${findings.length}`,statement:'Independent evidence items corroborate the same identity relationship.',confidence:edge.confidence||'medium',evidenceIds:[edge.from,edge.to],providers:edge.providers||[]});const identityNodes=nodes.filter(n=>n.type==='identity');for(const n of identityNodes.filter(x=>(x.providers||[]).length>1).slice(0,10))findings.push({id:`graph-finding:${findings.length}`,statement:`The identity “${n.label}” is supported by multiple providers.`,confidence:'high',evidenceIds:n.evidenceIds||[],providers:n.providers||[]});const confidence=findings.some(f=>f.confidence==='high')?'high':findings.length?'medium':'low';return {target,mode:'graph-grounded',summary:findings.length?`Graph analysis found ${findings.length} evidence-linked relationship(s), including ${corroborated.length} corroboration edge(s).`:'The graph does not contain enough corroborated evidence for a strong conclusion.',confidence,findings,evidenceCount:evidence.length,nodeCount:nodes.length,edgeCount:edges.length,limitations:confidence==='low'?['No cross-provider corroboration was established.']:[],generatedAt:new Date().toISOString()};}
