const USER_AGENT='AI-Intelligence-OS/1.0 (public intelligence research)';
const clean=v=>String(v||'').trim();
async function fetchJson(url){const r=await fetch(url,{headers:{accept:'application/json','user-agent':USER_AGENT}});if(!r.ok)throw new Error(`provider ${r.status}`);return r.json()}
export function personTarget(target){const t=clean(target);return t.startsWith('@')?t.slice(1):t}
export async function investigatePerson(target){const q=personTarget(target);const evidence=[];if(/^https?:\/\//i.test(q)){evidence.push({provider:'person-url',title:'Public person URL supplied',summary:'The investigation target is a public URL; additional enrichment requires the page provider to be explicitly supported.',url:q,retrievedAt:new Date().toISOString()});return {kind:'person',target,evidence,result:{confidence:'low',reason:'URL requires page-specific enrichment'}}}const wiki=`https://en.wikipedia.org/api/rest_v1/page/summary/${encodeURIComponent(q.replace(/\s+/g,'_'))}`;try{const w=await fetchJson(wiki);if(w.type==='standard'){evidence.push({provider:'wikipedia',title:w.title,summary:w.extract||'Public encyclopedia profile found.',url:w.content_urls?.desktop?.page||wiki,retrievedAt:new Date().toISOString()})}}catch{}return {kind:'person',target,evidence,result:{providers:['wikipedia'],confidence:evidence.length?'medium':'low'}}}
