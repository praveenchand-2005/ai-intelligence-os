import { investigateDomain } from './providers/domain.mjs';import { investigateEmail } from './providers/email.mjs';import { investigateWebsite } from './providers/website.mjs';import { investigateUsername } from './providers/username.mjs';import { investigateIp } from './providers/ip.mjs';import { investigatePhone } from './providers/phone.mjs';import { investigatePerson } from './providers/person.mjs';import { resolveEntity } from './entity-resolution.mjs';import { normalizeEvidence, providerSummary } from './evidence.mjs';
const email=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;const domain=/^(?:https?:\/\/)?(?:[a-z0-9-]+\.)+[a-z]{2,}$/i;const username=/^@?[a-zA-Z0-9._-]{2,64}$/;const ipv4=/^(?:\d{1,3}\.){3}\d{1,3}$/;const phone=/^\+[1-9]\d{7,14}$/;
function classify(value){if(email.test(value))return ['email',investigateEmail];if(/^https?:\/\//i.test(value))return ['website',investigateWebsite];if(ipv4.test(value))return ['ip',investigateIp];if(phone.test(value))return ['phone',investigatePhone];if(domain.test(value))return ['domain',investigateDomain];if(username.test(value))return ['username',investigateUsername];return ['person',investigatePerson]}
export async function collect(target){const value=String(target).trim();if(!value)throw new Error('target is required');const [kind,provider]=classify(value);const r=await provider(value);const evidence=normalizeEvidence(r.evidence||[]);return {kind:r.kind||kind,result:{...(r.result||r),entityResolution:resolveEntity(value,evidence),providerSummary:providerSummary(evidence)},evidence};}
