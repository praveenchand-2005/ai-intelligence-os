export function normalizeEvidence(items=[],fallbackProvider='unknown'){return items.filter(Boolean).map((e,i)=>({id:e.id||`${fallbackProvider}-${i}-${Date.now()}`,provider:e.provider||fallbackProvider,title:String(e.title||'Untitled evidence'),summary:String(e.summary||''),url:e.url||null,observedAt:e.observedAt||null,retrievedAt:e.retrievedAt||new Date().toISOString(),attribution:e.attribution||e.provider||fallbackProvider,raw:e.raw??null,meta:e.meta||{}}))}
export function providerResult(provider,result){return {...result,evidence:normalizeEvidence(result?.evidence||[],provider)}}
